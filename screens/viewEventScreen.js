import React from 'react';
import {View, StyleSheet} from 'react-native';

const viewEventScreen = props => {
    return (
        <View style={styles.main}>

        </View>
    );
};

const styles = StyleSheet.create({
    main: {},
});

export default viewEventScreen;