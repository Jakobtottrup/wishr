import React from 'react';
import {View, StyleSheet} from 'react-native';

const ViewHostScreen = props => {
    return (
        <View style={styles.main}>

        </View>
    );
};

const styles = StyleSheet.create({
    main: {},
});

export default ViewHostScreen;